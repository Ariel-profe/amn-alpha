export interface ICollabContent {
    id: string;
    title: string;
    text?: string;
}

export interface ICollabApps {
    id: string;
    title: string;
    icon: string;
    width: number;
    height: number;
}